import api from'@/src/service/apis/api';import type{WishlistItem,WishlistStatusItem}from'@/src/utils/types/wishlist.type';export const getWishlist=():Promise<WishlistItem[]>=>api.get('/wishlist').then(r=>r.data);export const getWishlistStatus=():Promise<{items:WishlistStatusItem[];productIds:string[]}>=>(api.get('/wishlist/status').then(r=>r.data));export const addWishlistItem=(productId:string,variantId?:string)=>api.post('/wishlist',{productId,variantId}).then(r=>r.data);export const removeWishlistItem=(id:string)=>api.delete(`/wishlist/${id}`).then(r=>r.data);
