import api from '@/src/service/apis/api';import type{ProductReview}from'@/src/utils/types/product.type';export const getReviews=(productId:string):Promise<{data:ProductReview[];total:number}>=>(api.get(`/products/${productId}/reviews`).then(r=>r.data));export const getReviewSummary=(productId:string):Promise<{averageRating:number;reviewCount:number;breakdown:Record<number,number>}>=>(api.get(`/products/${productId}/reviews/summary`).then(r=>r.data));export const getReviewEligibility=(productId:string)=>api.get(`/products/${productId}/reviews/eligibility`).then(r=>r.data);export const createReview=(productId:string,data:{rating:number;title?:string;comment:string})=>api.post(`/products/${productId}/reviews`,data).then(r=>r.data);export const updateReview=(productId:string,id:string,data:{rating:number;title?:string;comment:string})=>api.put(`/products/${productId}/reviews/${id}`,data).then(r=>r.data);export const deleteReview=(productId:string,id:string)=>api.delete(`/products/${productId}/reviews/${id}`).then(r=>r.data);export const getMyReviews=():Promise<ProductReview[]>=>api.get('/products/reviews/mine').then(r=>r.data);
