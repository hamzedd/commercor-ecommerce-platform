import{analyticsRange,comparison}from'./date-range';describe('analytics ranges',()=>{const now=new Date('2026-08-13T12:00:00Z');it('today uses hours',()=>expect(analyticsRange('today',undefined,undefined,now).bucket).toBe('hour'));it('previous duration matches',()=>{const r=analyticsRange('last_7_days',undefined,undefined,now);expect(+r.previousEnd-+r.previousStart).toBe(+r.end-+r.start)});it('custom validates',()=>expect(()=>analyticsRange('custom','x','y',now)).toThrow());it('zero comparison is neutral',()=>expect(comparison(2,0).percentageChange).toBeNull());it('percentage comparison',()=>expect(comparison(15,10).percentageChange).toBe(50))});
