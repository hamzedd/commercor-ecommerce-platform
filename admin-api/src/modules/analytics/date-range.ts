import{BadRequestException}from'@nestjs/common';export type AnalyticsRange={start:Date;end:Date;previousStart:Date;previousEnd:Date;bucket:'hour'|'day'|'week'|'month'};const day=86400000;export function analyticsRange(preset='last_30_days',start?:string,end?:string,now=new Date()):AnalyticsRange{let a:Date,b:Date=new Date(now);const midnight=(d:Date)=>new Date(Date.UTC(d.getUTCFullYear(),d.getUTCMonth(),d.getUTCDate()));switch(preset){case'today':a=midnight(now);b=new Date(a.getTime()+day);break;case'last_7_days':a=new Date(midnight(now).getTime()-6*day);b=new Date(midnight(now).getTime()+day);break;case'last_90_days':a=new Date(midnight(now).getTime()-89*day);b=new Date(midnight(now).getTime()+day);break;case'this_month':a=new Date(Date.UTC(now.getUTCFullYear(),now.getUTCMonth(),1));b=new Date(Date.UTC(now.getUTCFullYear(),now.getUTCMonth()+1,1));break;case'previous_month':a=new Date(Date.UTC(now.getUTCFullYear(),now.getUTCMonth()-1,1));b=new Date(Date.UTC(now.getUTCFullYear(),now.getUTCMonth(),1));break;case'custom':a=new Date(start||'');b=new Date(end||'');if(!Number.isFinite(+a)||!Number.isFinite(+b))throw new BadRequestException('Valid start and end dates are required');b=new Date(b.getTime()+day);break;default:a=new Date(midnight(now).getTime()-29*day);b=new Date(midnight(now).getTime()+day)}if(a>=b||b.getTime()-a.getTime()>day*732)throw new BadRequestException('Invalid analytics date range');const duration=b.getTime()-a.getTime(),previousEnd=new Date(a),previousStart=new Date(a.getTime()-duration);return{start:a,end:b,previousStart,previousEnd,bucket:preset==='today'?'hour':duration<=day*31?'day':duration<=day*120?'week':'month'}}export function comparison(current:number,previous:number){return{current,previous,difference:Number((current-previous).toFixed(2)),percentageChange:previous===0?null:Number(((current-previous)/previous*100).toFixed(2))}}
