import { Module } from '@nestjs/common'; import { JwtModule } from '@nestjs/jwt'; import { TypeOrmModule } from '@nestjs/typeorm';
import { JWT_SECRET } from '@/src/utils/environmentConstants'; import { UserEntity } from '@/src/libs/models/entities/user/User.entity'; import { LoyaltySettingsEntity } from '@/src/libs/models/entities/reward/LoyaltySettings.entity'; import { AuthGuard } from '@/src/libs/guards/auth.guard'; import { RoleGuard } from '@/src/libs/guards/role.guard';
import { RewardsController } from './rewards.controller'; import { RewardsService } from './rewards.service';
@Module({ imports: [JwtModule.register({ secret: JWT_SECRET, signOptions: { expiresIn: '1d' } }), TypeOrmModule.forFeature([UserEntity, LoyaltySettingsEntity])], controllers: [RewardsController], providers: [RewardsService, AuthGuard, RoleGuard] }) export class RewardsModule {}
