import adminApi from '../apiInstances/adminApi.ts';export type InventoryRow={productId:string;productName:string;variantId:string|null;variantDescription:string|null;sku:string|null;stock:number;threshold:number;status:string;lastMovementAt:string|null};export const getInventory=():Promise<InventoryRow[]>=>adminApi.get('/inventory').then(r=>r.data);export const adjustInventory=(data:unknown)=>adminApi.post('/inventory/adjust',data).then(r=>r.data);export const setInventory=(data:unknown)=>adminApi.post('/inventory/set',data).then(r=>r.data);export const getMovements=()=>adminApi.get('/inventory/movements').then(r=>r.data);
