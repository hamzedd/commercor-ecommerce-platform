import api from '../apiInstances/adminApi.ts';import type{VariantData,VariantInput}from'../../utils/types/productVariantTypes.ts';export const getVariants=(id:string):Promise<VariantData>=>api.get(`/products/${id}/variants`).then(r=>r.data);export const addVariantOption=(id:string,data:{name:string;values:string[]}):Promise<VariantData>=>api.post(`/products/${id}/variant-options`,data).then(r=>r.data);export const createVariant=(id:string,data:VariantInput)=>api.post(`/products/${id}/variants`,data).then(r=>r.data);export const updateVariant=(id:string,variantId:string,data:VariantInput)=>api.put(`/products/${id}/variants/${variantId}`,data).then(r=>r.data);export const deleteVariant=(id:string,variantId:string)=>api.delete(`/products/${id}/variants/${variantId}`).then(r=>r.data);
