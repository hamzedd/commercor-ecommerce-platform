# HTTP
PORT=3001

# PostgreSQL (normally the same database used by admin-api)
DB_TYPE=postgres
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=replace_with_local_password
DB_NAME=commercor

# Authentication
JWT_SECRET=replace_with_a_long_random_local_secret

# Customer-web locale base URL returned for payment status links
DOMAIN_URL=http://localhost:3002/en
