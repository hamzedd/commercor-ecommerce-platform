export type WishlistIdentity={customerId:string;productId:string;variantId?:string|null};export function wishlistKey(v:WishlistIdentity){return `${v.customerId}:${v.productId}:${v.variantId||''}`}export function ownsWishlistItem(customerId:string,item:{customerId:string}){return customerId===item.customerId}export function shouldSelectVariant(product:{hasVariants:boolean},variantId?:string|null){return product.hasVariants&&!variantId}
