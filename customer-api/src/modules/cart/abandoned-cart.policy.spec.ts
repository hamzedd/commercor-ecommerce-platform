import { CartStatus } from '@/src/libs/models/entities/cart/Cart.entity'; import { isAbandonmentEligible } from './abandoned-cart.policy';
describe('abandoned cart policy',()=>{const now=new Date('2026-01-02T12:00:00Z');const base={status:CartStatus.ACTIVE,lastActivityAt:new Date('2026-01-02T10:00:00Z'),checkoutOrderId:null,itemCount:1,recoveryEmailSentAt:null};it('abandons an old active nonempty cart',()=>expect(isAbandonmentEligible(base,now,60,24)).toBe(true));it('keeps recent carts active',()=>expect(isAbandonmentEligible({...base,lastActivityAt:new Date('2026-01-02T11:30:00Z')},now,60,24)).toBe(false));it('ignores empty and converted carts',()=>{expect(isAbandonmentEligible({...base,itemCount:0},now,60,24)).toBe(false);expect(isAbandonmentEligible({...base,status:CartStatus.CONVERTED},now,60,24)).toBe(false)});it('prevents pending checkout false positives',()=>expect(isAbandonmentEligible({...base,checkoutOrderId:'order'},now,60,24)).toBe(false));it('enforces cooldown and permits a later cycle',()=>{expect(isAbandonmentEligible({...base,recoveryEmailSentAt:new Date('2026-01-02T00:00:00Z')},now,60,24)).toBe(false);expect(isAbandonmentEligible({...base,recoveryEmailSentAt:new Date('2025-12-31T00:00:00Z')},now,60,24)).toBe(true)})});
