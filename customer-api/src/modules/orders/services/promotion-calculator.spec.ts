import{calculatePromotions}from'./promotion-calculator';import{PromotionDiscountType as D,PromotionType as T}from'@/src/libs/models/entities/promotion/Promotion.entity';
const rule=(x:any)=>({id:x.id||'1',name:'Sale',type:T.ORDER,discountType:D.PERCENTAGE,discountValue:10,priority:0,stackable:true,productIds:new Set<string>(),categoryIds:new Set<string>(),...x});
describe('promotion calculator',()=>{const lines=[{productId:'p',categoryId:'c',quantity:3,unitPrice:10}];it('percent and cap',()=>expect(calculatePromotions(30,lines,[rule({maximumDiscount:2})],5).merchandiseDiscount).toBe(2));it('fixed clamps',()=>expect(calculatePromotions(30,lines,[rule({discountType:D.FIXED,discountValue:99})],5).discountedSubtotal).toBe(0));it('product/category target',()=>expect(calculatePromotions(30,lines,[rule({type:T.CATEGORY,categoryIds:new Set(['c'])})],5).merchandiseDiscount).toBe(3));it('bogo lowest units',()=>expect(calculatePromotions(30,lines,[rule({type:T.BOGO,buyQuantity:2,getQuantity:1,getDiscountPercent:100,productIds:new Set(['p'])})],5).merchandiseDiscount).toBe(10));it('free shipping',()=>expect(calculatePromotions(30,lines,[rule({type:T.FREE_SHIPPING})],5).shippingDiscount).toBe(5));it('non-stackable wins by priority',()=>expect(calculatePromotions(30,lines,[rule({id:'a',priority:2,stackable:false}),rule({id:'b',discountValue:50})],5).promotions).toHaveLength(1));});
