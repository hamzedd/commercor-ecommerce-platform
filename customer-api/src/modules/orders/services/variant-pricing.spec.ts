import{cartLineKey,effectiveVariantPrice}from'./variant-pricing';describe('variant pricing',()=>{it('uses price override',()=>expect(effectiveVariantPrice(20,25)).toBe(25));it('falls back to product price',()=>expect(effectiveVariantPrice(20,null)).toBe(20));it('keeps variants as separate cart identities',()=>{expect(cartLineKey('p','black')).not.toBe(cartLineKey('p','white'));expect(cartLineKey('p','black')).toBe(cartLineKey('p','black'))});});
